namespace Utilities.Auth;

/// <summary>Token type constants used in authorization headers and token responses.</summary>
public static class TokenTypes
{
    /// <summary>RFC 6750 Bearer token type.</summary>
    public const string Bearer = "Bearer";
}
